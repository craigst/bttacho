package com.tacho.downloader

/**
 * Destination endpoint for parsed reports.
 *
 * Intentionally blank in version control -- set it locally, or supply it via
 * local.properties / a build config field, so no private infrastructure
 * hostname ships in the repository.
 */
const val WEBHOOK_URL = ""

const val USB_CCID_CLASS = 0x0b
